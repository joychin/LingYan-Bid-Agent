# 编写任务与 Artifact 系统 · 设计方案（v1.0）

> **文档状态**：P1–P3 已实施；P4 任务层已实施（2026-08-25，见 §15）。本方案是
> 任务层 + 类型化 Artifact + 客户端 Processor 的设计与决策唯一事实来源；
> 与 `tender-agent-mvp-prd.md` 冲突处以本文为演进方向。
>
> **产生方式**：2026-08-24 产品设计讨论收敛而成，全部关键决策均经逐项确认，修正项附理由；
> P4 业务模型于 2026-08-25 第二轮讨论定稿（§15）。

---

## 0. 背景与问题

**现状**：Agent 在会话中产出 JSON/HTML/Markdown 文件，前端按扩展名做通用预览（JSON 即格式化文本）。用户想调整目录顺序，只能回到聊天里用自然语言指挥 LLM——比自己动手慢得多。

**目标体验**：用户点击任何产物，打开的是针对该类产物的**专用工作界面**，直接上手改，改完即生效，并成为后续 AI 工作的正式输入。

例：点击"投标目录" → 打开可拖拽的目录编辑器；点击"评分矩阵" → 打开表格编辑器。用户永远不面对原始 JSON。

---

## 1. 产品定位与三条铁律

**一句话定位**：用户和 AI 共同加工"业务成果"——AI 出初稿，用户直接上手，改好的成果是下一步的原料。

**第一性原则**：用户选择手动调整，是因为"自己改比指挥 LLM 快"。一切机制都必须服务这个速度，而不是管理这个速度。

三条产品铁律，全部机制围绕它们展开：

1. **每个成果只有一份当前内容。** 用户和 LLM 修改的都是它，改完即保存，没有版本树、没有"保存并设为当前版本"。
2. **更新主导权归用户（文件夹语义）。** 发布即覆盖当前内容（覆盖前自动留恢复点）；用户屏幕上的编辑副本永不被清，内容冲突由用户二选一裁决（拉取最新 / 保留我的）。不做任何后台锁或互斥。
3. **LLM 永远不能静默覆盖用户的工作。** LLM 对已有成果的修改必须经用户采纳（或用户明确委托）才写入；覆盖发生时必有恢复点与提示。

**最重要的整体取舍**：主动放弃版本管理与冲突合并。版本树、Diff、合并对用户是认知负担，对开发是成本黑洞。用"单一当前内容 + 文件夹覆盖语义 + 用户裁决"更简单的规则替代。

---

## 2. 核心概念模型

```
Task Template（内置模板目录，随客户端发布）
      │ 声明：本任务跟踪哪些 Artifact Contract
      ▼
Task（编写任务 = 一个投标项目，唯一工作上下文）
 ├── Thread A / Thread B / …（对话线程，各自的对话记忆）
 └── 共享成果区
      ├── 投标目录        (tender.directory)          → 目录编辑器
      ├── 需求拆解清单    (tender.requirement-matrix) → 清单编辑器
      ├── 评分矩阵        (tender.scoring-matrix)     → 矩阵编辑器
      └── 章节草稿 ×N     (tender.section-draft)      → 章节编辑器
                │
                ▼ 由 Contract 决定
平台契约目录（Contract Registry，服务端持有真值）
客户端处理器注册表（Processor Registry，kind+schema → 界面）
```

| 概念 | 是什么 | 由谁维护 | 生命周期 |
|---|---|---|---|
| Task Template | 某类编写任务跟踪哪些成果类型 | 内置目录，随客户端发布 | 相对稳定 |
| Contract | 一个业务类型的完整定义（结构+行为声明） | 平台级，真值在 sidecar | 随版本演进 |
| Task | 一个项目的容器 | 运行时创建 | 长期 |
| Thread | 讨论入口，有独立对话记忆 | 运行时创建 | 长期 |
| Artifact | 一个业务对象的当前状态 | 运行时数据 | 跟随任务 |
| Processor | 某类型成果的专用界面 | 客户端内置 | 随客户端发布 |

关键归属关系：**成果属于任务，不属于线程。** 线程只是产生和修改成果的入口之一。

---

## 3. Artifact Contract（平台契约目录）

Contract 是整个类型系统的原子，每个契约声明：

```
kind              业务类型标识，如 tender.directory
schema            数据结构标识 + 版本，如 tender-response-docs@1
cardinality       task-single（任务内一份）| task-multi（任务内多份）
llm_write_mode    suggest（LLM 修改走候选采纳）
                  | direct-on-request（用户明确委托时可直接改写）
editable          用户是否可手动编辑（只读契约如来源登记表为 false）
```

**cardinality 示例**：投标目录 = task-single（多分册"技术部分/商务部分"是 schema 内部结构）；章节草稿 = task-multi（每章一份）。

**llm_write_mode 示例**：目录、矩阵这类结构化数据用 suggest（候选/建议均可表达）；章节正文这类成段文字用 direct-on-request（"帮我重写这章"无法拆成逐条建议）。

**治理原则**：

- 类型只能由平台注册，LLM 与 Skill 都不能发明新类型。
- **契约真值只存一份**——sidecar 的校验模型（Pydantic）。客户端只维护"kind+schema → Processor"的映射和防御性解析，不重复建模。sidecar 通过 API 暴露契约目录元数据，客户端启动时对账、报告未覆盖的契约。
- **schema 版本是判别符**：出现 `@2` 视为新契约，旧任务继续用旧 Processor，不做自动迁移。

---

## 4. Artifact 存储模型

### 4.1 目录结构（内容与身份分离；§16 任务分组布局）

包按所属任务分组存放（目录即命名空间，包内结构不变）：

```
data/workspace/<task_id>/
 ├── formal/art_<id>/          任务正式稿包（转正目标）
 │    ├── manifest.json        身份卡：发布时写一次，此后基本不动
 │    └── current/content.json 当前内容：唯一工作版本，自动保存的对象
 ├── threads/<conv_id>/art_<id>/   各会话过程稿包（结构同上，含 restorepoints/）
 ├── files/                    任务输入文件（上传区，见 §16）
 ├── out/                      技能工作台（parse/analysis/outline，任务级）
 └── drafts/                   LLM 发布草稿区（任务级）
```

### 4.2 manifest（稳定身份）与运行态（易变状态）严格分离

manifest 只放：`artifact_id / task_id（恒为所属任务，过程稿经会话反查写入）/ conversation_id（None=正式稿）/ display_name / kind / schema / content_type / cardinality / 首次发布来源（skill, thread, run）/ derived_from / created_at`。包的磁盘位置是 `(artifact_id, manifest scope)` 的纯函数。

**不放**：谁在编辑、何时更新、候选状态——这些放 sidecar 内存 + SQLite 索引，重启后自然复位。

> 修正记录：最初设计把 `current_editor / updated_at` 放进 manifest，导致自动保存高频重写身份卡，既浪费又扩大崩溃窗口。收窄为只写一次的稳定身份。

### 4.3 权威与索引

**manifest 是权威**：Artifact 包自描述，SQLite 丢失或不一致时可扫描重建（扫描范围 = 各任务目录的 `formal/*/` 与 `threads/*/*/`，跳过 skills/archive 等全局目录）。**SQLite 只是查询投影**（列表、过滤、当前状态）+ 运行态。代价（每次发布/修订要保证内容与索引一致）换来：成果可迁移、可恢复、身份不依赖数据库。

### 4.4 内部保护

- **content_seq**：内容版本号。角色是**探测器而非锁**——编辑器轮询比对它发现外部更新，保存携带它探测并发写入；不匹配时返回 409 触发用户裁决（拉取最新 / 保留我的=force 覆盖），而不是拒绝用户。
- **恢复点**：覆盖"他人内容"前自动留底（触发点：发布覆盖当前内容、用户 force 保留自己的版本、恢复操作本身），保留最近 3 个，配套「恢复上一版」一键按钮（恢复本身也留底、可再撤销）。是安全网，不是版本管理——无版本列表、无 Diff、无分支。

---

## 5. 发布管线（成果如何产生）

**"发布后共享"**：线程内的中间产物先是草稿（仅本次 run 可见）；只有正式发布，才进入任务共享成果区。

```
Skill 内 LLM 分析
 → 写草稿 <task_id>/drafts/<run_id>/*.json     （草稿，不是成果；任务级草稿区）
 → 调用 publish_artifact(contract, draft_path, display_name)
 → 服务端校验链（全部硬约束）：
     ① contract 在平台契约目录中存在
     ② 当前任务模板声明跟踪该 contract     ← 授权锚点
     ③ 草稿内容通过该 contract 的 schema 校验
     ④ 草稿路径在本 run 草稿区内（containment）
 → 原子落盘（tmp + rename）→ 写 manifest → 更新索引
 → 发布 artifact.created（携带类型信息，任务内所有线程可见）
```

### 5.1 授权锚点 = 任务模板，而不是 Skill

**修正记录**：最初设计为"每个 Skill 只能发布自己那几类成果"，但 DeepAgents 工具全局注册，服务端无法验证调用来自哪个 Skill，这种授权只是提示级约束。改为"任务模板声明跟踪哪些契约"后，`task → template → 契约集合` 是服务端持有的事实，无法伪造；语义错标（目录内容标成矩阵）由 schema 校验兜底。Skill 与契约的对应关系退化为 SKILL.md 里的指导性知识。

类比：项目档案柜定死了有几个抽屉，谁来归档都只能放进这几个抽屉，放错柜子会弹回去。

**LLM 的自由度**：在任务认可的类型集合内，一次执行可以选择发布零到多个成果——"受控选择"，保留 Agent 灵活性，守住类型系统。

### 5.2 导出物不是成果

打印视图 HTML、导出 docx 等一律是单向派生的文件，落导出目录，不进成果列表、不参与类型系统。原 `out/` 扫描发现机制整体废弃（不考虑兼容旧产物），Publisher 是唯一登记入口。类比：合同打印出来的纸质版不是合同的源头。

---

## 6. 客户端：识别与唤起

```
用户点击成果
 → 读取 manifest（kind + schema）
 → Artifact Open Host 加载 current 内容
 → Resolver 查客户端 Processor Registry
 → 命中 → 唤起对应 Processor
 → 未命中 → 明确提示"当前客户端不支持该类型"（不做 JSON 兜底预览）
```

- **Open Host** 是通用容器：开关、加载态、标题栏（名称/类型/编辑状态）、错误处理、通用动作（在文件夹中显示等）。不感知任何领域结构。
- **Processor** 是业务界面：目录编辑器知道树、拖拽、分册；矩阵编辑器知道行列、权重、总分校验。各自的解析、渲染、编辑、校验、序列化逻辑完全内聚。
- 文件名只是显示名，不参与匹配。任何项目、任何线程、任何修改状态的"目录"，打开的都是同一个目录编辑器。

---

## 7. 编辑模型

### 7.1 用户编辑：直接改，自动存

- 打开 Processor 即加载当前内容，用户在内存副本上操作；调整后短防抖自动保存（原子替换 current + 更新索引），全程只显示轻量"已保存/保存中"状态。
- 不弹保存确认，关闭即走，不存在"未保存警告"。
- 保存失败：重试 + 状态提示，**永不回滚用户输入**。

### 7.2 并发与冲突：文件夹语义，用户裁决（无租约）

> 修正记录（修订 2）：最初设计为"租约制编辑权（用户心跳续租、LLM run 持有、任务级互斥）"。用户以第一性原则纠正——协调机制正是砍掉版本管理时要避免的东西；run 的绝大部分时间在读取而非写入，为罕见的写冲突惩罚所有正常并发（如"一个线程写目录、另一个问废标项"）是反直觉的。且编辑器里的本地副本永远不会被别人清掉，不存在"工作丢失"，只存在"两个版本谁留"的选择——把这个选择交给人即可。

最终规则（全部站在用户这边）：

```
1. 并发随便跑 —— 任务内多线程同时工作，无任何运行互斥、无编辑租约
2. 发布即覆盖 —— AI 发布直接覆盖当前内容（文件夹语义：重新上传同名文件），
                  覆盖前自动留恢复点
3. 冲突由用户裁决 —— 编辑器轮询探测外部更新（content_seq 是探测器，不是锁）：
   · 无本地改动 → 静默跟随最新内容
   · 有本地改动 / 保存撞上外部更新 → 提示二选一：
     「拉取最新内容」（换基底）或「保留我的版本」（强制覆盖，对方版本留底）
```

- 关闭编辑器时冲刷挂起保存；若冲刷撞上外部更新，按「主导权归用户」强制保留用户版本。
- schema 校验始终执行（force 也不豁免）；恢复点保留最近 3 个，作为唯一安全网。
- 内部不可见 plumbing 仅有：文件原子落盘（tmp+rename）、发布进程内写锁（防并发首建产生重复成果）。同会话仍单 run——那是同一 checkpoint 的对话完整性，不属于共享成果协调。

### 7.3 LLM 修改已有成果：两种模式

**suggest（结构化成果，默认）—— 一期实现"候选 → 采纳"**：

```
用户在成果上发起"让 AI 调整"
 → LLM 读取当前内容 → 产出完整候选（过 schema 校验）
 → 存为 pending_candidate（挂在成果上，不碰 current）
 → Processor 内并排预览：当前 vs 候选
 → 用户整体采纳（替换 current）/ 丢弃
 → 同一时刻只保留一个候选，新候选覆盖旧候选
```

若用户在候选生成期间继续手动编辑，候选基于的 content_seq 已过期 → 标记"内容已变化，请重新生成"，**丢弃重来，不做合并**。

二期演进为条目级建议（"建议把售后服务挪到技术方案下"逐条勾选采纳）——pending_candidate 挂点为它预留，存储模型不变。

> 修正记录：条目级建议需要每种契约定义一套操作 schema + 每种 Processor 做逐项采纳 UI，是全方案最贵组件。一期先用"整份候选"廉价验证原则（用户决定是否写入），原则不变。

**direct-on-request（成段文字）**：用户明确委托"重写这一章"时直接改写 current，写回基于 content_seq 探测（撞上外部更新时同样交用户裁决）。

---

## 8. 任务与线程模型

### 8.1 实体与模板

- **Task 为新实体**：task_id、template_id、title、任务事实（项目名称、招标编号等结构化小体量信息）。
- **Thread 复用现有 conversation**，增加 task_id 归属。侧栏从"会话平铺"升级为"任务 → 会话"两级（导航形态留待实施前细化）。
- **Task Template 是内置目录**：如"招标响应书"模板声明跟踪 [目录、需求清单、评分矩阵、合规清单、章节草稿]。任务实例继承该集合——即"任务定义好共享的 Artifact 有哪些"。模板声明"最多跟踪哪些"，不要求全部立即存在。

### 8.2 共享上下文语义：共享成果与结论，不共享聊天记录

> 修正记录：最初设想"任务内线程共享统一上下文"。若实现为共用一份完整对话记忆/checkpoint，并发线程会互相踩踏（共享 checkpoint 并发写是已知崩溃模式）。修正为：

```
任务层（共享）                    线程层（隔离）
├── 任务事实                      └── 各自独立的对话记忆
├── 全部成果的当前内容                （checkpoint 维持按线程隔离）
└── [决策纪要：二期引入]
```

- 每次 run 启动时自动注入：**任务事实 + 成果清单**（类型与名称，不含全文）；AI 需要具体内容时通过 `read_artifact(contract)` 按需读取当前内容。
- 类比：同事之间共享的是文档和结论，不是彼此的聊天记录。"换一个线程继续"延续的是工作状态，不是对话。
- **一期不做决策纪要**。任务事实来源：建任务时用户填写 + skill 发布成果时从 meta 同步（tender-response-docs 的 meta 本就含项目名称/招标编号），无需独立维护机制。二期引入纪要时注入点相同、语义不变。

### 8.3 并发与删除

- **run 并发完全放开**：同一任务多个线程可同时运行（一个写目录、一个问答互不干扰）。共享写入点的保护是隐形的：发布覆盖自动留恢复点 + 发布进程内写锁防重复成果 + 编辑器轮询探测交用户裁决。同会话仍单 run（同一 checkpoint 的对话完整性）。
- **删除语义**：删线程删对话记忆与该线程过程稿目录；删任务则**整个任务目录软归档**（threads/ 先硬删，其余 mv 到 `workspace/archive/<task_id>/`，可手工找回），不硬删。

---

## 9. 后续消费（成果如何喂给下一阶段）

后续阶段不依赖文件名，按契约引用：

```
需求分析阶段
  输入：任务的 tender.directory 当前内容
  输出：发布 tender.requirement-matrix（manifest 记录 derived_from）
```

- 任何线程里的 AI 读到的都是成果的**当前内容**——用户手动调整的结果天然成为后续输入，这是整个系统业务价值的闭环点。
- manifest 的来源字段（首次发布来源 + derived_from）保留最薄的谱系，满足"这个成果哪来的"的追溯，不构建版本树。

---

## 10. 关键决策记录

| # | 决策 | 理由 |
|---|---|---|
| 1 | 产物是类型化业务对象，不是文件 | 文件名/扩展名承载不了业务语义 |
| 2 | 内容与 manifest 分离 | 业务数据不被基础设施字段污染 |
| 3 | manifest 为权威，SQLite 只是可重建索引 | 成果可迁移可恢复，身份不依赖库 |
| 4 | manifest 只含稳定身份（修正） | 易变状态高频重写身份卡是浪费和风险 |
| 5 | 不考虑兼容性，废弃 out/ 扫描 | Publisher 成为唯一登记入口 |
| 6 | 授权锚点 = 任务模板（修正，原为 Skill） | Skill 级授权技术上不可硬约束；任务级可执行 |
| 7 | 草稿 → 发布，发布后共享 | 任务工作台不被试验/失败中间结果污染 |
| 8 | LLM 在任务认可集合内受控选择 | 保留 Agent 灵活性，守住类型系统 |
| 9 | 用户直接编辑 + 自动保存，无版本树 | 回到"用户自己改更快"的第一性原则 |
| 10 | 并发冲突：文件夹覆盖语义 + 用户裁决（修正，原为租约互斥） | 协调机制是砍版本管理时要避免的东西；本地副本永不丢，冲突只需用户二选一 |
| 11 | LLM 修改默认 suggest；一期候选/采纳（修正） | 条目级建议最贵，先廉价验证原则 |
| 12 | 契约声明 cardinality / llm_write_mode / editable（修正） | 一份还是多份、建议还是直改，不定则界面无从画 |
| 13 | 契约真值在 sidecar，客户端只做映射（修正） | 避免 Python/TS 双份维护漂移 |
| 14 | 导出物退出成果体系（修正） | 打印件不是源头 |
| 15 | 共享上下文 = 线程独立 checkpoint + 任务注入（修正） | 共用记忆会并发踩踏；共享结论更贴近真实协作 |
| 16 | 任务内 run 并发放开（修正，原为任务级互斥） | run 绝大部分时间在读取；为罕见写冲突惩罚正常并发反直觉 |
| 17 | 新任务实体 + 内置模板目录 + 平台契约目录 | 任务定义共享成果，是授权与导航的锚点 |

补充决策（定稿阶段）：

| # | 决策 | 理由 |
|---|---|---|
| 18 | 一期不做决策纪要 | 任务事实已有来源（建任务填写 + meta 同步），纪要二期按相同注入点引入 |
| 19 | 契约跟随 skill 接入节奏 | 每个契约随所属 skill 改造打包接入，不预设固定首批清单 |
| 20 | 发布覆盖前自动留恢复点 + 「恢复上一版」 | 覆盖语义的安全网：错了能一键回去，且恢复可再撤销 |

---

## 11. 与现状的差距

| 现状 | 目标 |
|---|---|
| 只有会话，无任务层 | Task / Task Template 两级容器 |
| 成果靠 out/ 扫描发现，无类型 | Publisher + Contract 声明类型 |
| 成果全局平铺，删会话即解绑 | 归属任务，任务内线程共享 |
| 点击即扩展名预览 | Open Host → Resolver → Processor |
| 产物一次性写死，不可编辑 | 当前内容 + 自动保存 + 用户裁决 |
| 后续阶段读固定文件名 | 按契约读任务当前内容 |
| SSE artifact.created 只带文件名/格式 | 携带契约信息 |

---

## 12. 开放问题状态

| # | 问题 | 状态 |
|---|---|---|
| 1 | 任务事实与决策纪要由谁维护 | ✅ 已决：一期不做纪要，事实来自建任务填写 + meta 同步 |
| 2 | 首批契约范围 | ✅ 已决：跟随 skill 接入节奏（tender.directory 随 tender-toc 首个接入） |
| 3 | 侧栏两级导航的具体形态 | 留待任务层实施前细化（交互细节，不阻塞设计） |
| 4 | schema @2 的实际演进流程 | 留待首次真实升级（原则已定：版本即判别符，不做自动迁移） |

---

## 13. 新契约接入标准清单

契约跟随 skill 改造节奏接入，每次接入是一个**打包动作**，照单执行：

```
接入一个新契约（随 skill 改造）
 1. sidecar：契约定义 —— kind / schema / Pydantic 校验模型
            / cardinality / llm_write_mode / editable
 2. sidecar：更新该 skill 的 SKILL.md —— 指导性说明可发布哪些契约
 3. 客户端：实现 Processor 并注册进 Registry
 4. 模板：更新相关任务模板的契约集合
    （模板声明"最多跟踪哪些"，不要求全部立即存在）
 5. 验收：发布 → 唤起 → 编辑 → 后续消费 全链路走通
```

不允许出现"契约注册了但客户端没有 Processor"的半接入状态（客户端启动对账会报警）。

---

## 14. 验证顺序（非实施计划）

```
P1  契约骨架 + 目录契约（随 tender-toc skill 改造）
P2  垂直切片：发布管线 + 成果存储 + 类型化事件 + 客户端唤起 + 目录只读视图
P3  编辑闭环：自动保存 + content_seq 探测 + 用户裁决覆盖 + 恢复点
P4  任务层：任务实体 + 模板 + 线程归属 + 任务注入（无运行互斥）
P5  LLM 候选/采纳通道
P6  条目级建议、导出丰富化（按需）
```

顺序逻辑：**先验证内核（类型化成果 + 专用界面能否跑通），再搭容器（任务层），最贵的最后做。** P2–P3 不依赖任务层，可在现有会话作用域内验证，避免一次性重写导航、路由与数据模型。

---

## 15. P4 任务层（已实施，2026-08-25）

P4 按下方定稿实施。与 §8 原设计的差异：任务模板实体**未引入**（单场景期授权 stub 维持
「已注册契约全允许」，模板待第二个场景出现时再引入）；「决策纪要」以**进度便签**的更简形态落地。

### 15.1 业务模型六条（第二轮讨论定稿）

1. **任务 = 一次投标 = 一个项目文件夹**。会话永远挂在任务下，不存在无任务归属的会话
   （POST /conversations 必填 task_id）；侧栏从按日期分组升级为「任务 → 会话」两级文件夹树。
2. **共享上下文 = 任务名 + 进度便签 + 产物清单**（任务全部正式稿 + 本会话过程稿，只含
   名称与 artifact_id），每次**模型调用**经 `_TaskContextMiddleware` 现算注入 system message
   ——不落 checkpoint、run 中途发布的成果下一次调用即可见。聊天记忆仍按会话隔离（§8.2）。
3. **产物两层文件夹**：任务「正式稿」（task_id 非空）/ 会话「过程稿」（conversation_id 非空），
   同一套契约与编辑机制；右栏产物面板两层切换。
4. **转正 = 两个入口、一个关卡**：AI 可发起（发布过程稿时挂 `promotion_proposed` 建议标记 →
   界面高亮「建议转正」卡），用户点「转正」确认后才复制进正式稿；用户也可对任何过程稿手动
   转正。**正式稿的每次写入背后都有用户一次点击**（铁律三的落地）——LLM 发布工具永远无法
   直写正式稿（publish scope 仅供 promote 端点内部使用）。转正=复制（过程稿原件保留）；
   task-single 正式稿已有同类则覆盖 + 恢复点，manifest 记 `derived_from` 最薄谱系。
   确认形态是「建议卡」而非「打断运行弹窗」——运行照常跑完，不因用户离开而阻塞。
5. **未登记类型收拢为通用笔记 `doc.note/note-md@1`**（title + body_md，task-multi）：
   LLM 想存未注册内容一律存成笔记（发布工具对未知契约自动收拢并强制文档形态），
   任务记忆不因类型未登记而中断；类型系统保持平台封闭注册；客户端配通用文档
   Processor（markdown 查看/编辑 + 409 冲突裁决，不做轮询）。
6. **删除语义**：删会话 = 删对话记忆 + 该会话过程稿目录（`threads/<conv_id>/` + 索引）；
   删任务 = 会话全部删除（含记忆），**整个任务目录软归档**至 `workspace/archive/<task_id>/`
   （threads/ 过程稿先硬删；正式稿/上传文件/out/ 工作台随归档可手工找回）。

辅助机制：**进度便签**是 tasks.progress_note 一张 markdown 白板（LLM 经
`update_task_progress` 工具更新、用户可在产物面板任务页签编辑，同一份真值，
last-write-wins 非版本化）；`read_artifact` 读取顺序 = 任务正式稿 → 本会话过程稿
（§16 起两级），task-multi 类型可用 artifact_id 指定。

### 15.2 关键实现锚点

- **sidecar**：`db`（tasks 表；conversations.task_id、artifact_index.conversation_id/
  promotion_proposed 由 PRAGMA 探测 ALTER 补列——建表与建索引拆两步，索引引用新列）；
  `app/api/tasks.py`（CRUD；POST 默认连带第一个会话，`with_conversation=false` 供输入区
  选择器就地新建）；`app/api/artifacts.py` 的 `POST /artifacts/{aid}/promote`（不发 SSE——
  不在 run 内 seq 契约无宿主，前端 promote 成功后自行刷新）；`runctx` 增加 task_id；
  `events.artifact_created_payload` 统一 run 边界与转正的载荷（additive：scope/task_id/
  promotion_proposed）；删任务在 API 层先做磁盘归档（§16 archive_task）再删库
  （索引行随 db 级联清理，db 不感知文件系统）。
- **frontend**：`TaskPicker`（新会话必选所属任务，可就地新建）；产物面板两层 tab +
  过程稿行内「转正」；`NoteProcessor`；侧栏任务树（任务文件夹 hover 新会话 + 重命名/删除）；
  欢迎页无任务时引导创建第一个任务。
- **老数据**：用户拍板清空重来（删除 data/ 重建），不写数据迁移。

### 15.3 已知限制（P5+ 候选）

- 任务模板实体未引入（授权 stub：已注册契约全允许）。
- 跨会话的正式稿变更无实时推送（其他会话刷新/切页可见）。
- 同一 task-multi 笔记多次转正会产生多份正式稿副本（文件夹语义，不按 derived_from 去重）。

---

## 16. 任务分组目录布局（已实施，2026-08-25）

P4 的「任务 = 项目文件夹」从比喻升级为**字面路径事实**：workspace 按任务分组，
目录即命名空间。本节是磁盘布局的唯一事实来源（§4.1/§8.3 的修订在此汇总）。

### 16.1 布局与派生规则

```
data/workspace/
  <task_id>/                    任务 = 一切归它（目录名只用不可变 id，不随改名变）
    formal/art_<id>/            正式稿包（包内结构不变：manifest + current/ + restorepoints/）
    threads/<conv_id>/art_<id>/ 各会话过程稿包
    files/                      任务输入文件（上传区）
    out/                        技能工作台：parse/ analysis/ outline/（任务级，跨会话共享）
    drafts/                     LLM 发布草稿区（10MB/containment 规则不变）
  skills/                       全局技能（FilesystemBackend root = 整个 workspace，不变）
  archive/<task_id>/            删任务软归档 = 整任务目录 mv
```

- **包位置 = `(artifact_id, manifest)` 的纯函数**：manifest 的 `task_id` 恒为所属任务
  （过程稿发布时经会话反查写入）；`conversation_id` 非空 → `threads/` 下，为空 → `formal/`
  下。store 层全部路径函数 `(aid, scope)` 签名，scope 即 db 索引行或 manifest。
- **目录按需创建**（首次上传/解析/发布时 mkdir），任务创建不预建骨架。
- **删除**：删会话 = rmtree `threads/<conv_id>/` + 索引行（db 级联）；删任务 = 先 rmtree
  `threads/`（过程稿硬删），再 mv 整任务目录到 `archive/<task_id>/`（正式稿+files+out
  随归档可手工找回），409 活跃 run 守卫与记忆清理不变。

### 16.2 任务级文件区（上传）

- `POST /api/files?task_id=`（缺参 422、任务不存在 404）落 `<task>/files/`，白名单/
  100MB/任务内同名覆盖语义不变；`GET /api/files?task_id=` 只列该任务；DELETE 同理。
  **跨任务同名文件互不影响**（§15.3 旧限制清除）。
- 前端上传上下文携带当前任务（选中会话 → 其所属任务；草稿页 → 选择器挑的任务），
  无任务时上传被拒绝并提示先选任务（回形针禁用 + 欢迎页/拖拽 toast 引导）。

### 16.3 模型可见性（任务工作目录注入）

- FilesystemBackend root 保持整个 workspace：模型路径带任务段（如 `<task>/out/parse/...`）。
- `_TaskContextMiddleware` 的任务上下文块注入「任务工作目录：`<task_id>/`（files/ 上传区、
  out/ 工作台、drafts/ 草稿区；引用路径带该前缀）」；skills 的 SKILL.md 以同一约定表述。
- `parse_document` 产物落 `<task>/out/parse/`，返回路径已含任务段（模型直接沿用）；
  裸文件名回退：workspace 根找不到时到当前任务 `files/` 按文件名找。

### 16.4 全局作用域移除

`task_id/conversation_id` 皆空的 P4 前遗留语义彻底删除：publish 必须恰传其一
（会话作用域经会话反查所属任务恒写 task_id）；read_artifact 读序两级；
db 的 NULL/NULL 查询分支删除；前端 `scope` 类型收窄为 `task | conversation`。

### 16.5 决策记录

| # | 决策 | 理由 |
|---|---|---|
| 1 | 物理目录 = 业务结构（路径即命名空间） | 扁平布局 + db 索引归属曾致多任务串台（out/analysis 全局单份被覆盖） |
| 2 | art 包保留包格式只挪位置 | manifest 权威/恢复点/转正/契约体系是地基，不拆 |
| 3 | 目录名只用不可变 id | 任务改名不联动 mv；标题只在 UI |
| 4 | out/ 与 drafts/ 任务级 | 分析成果跨会话共享（§8.2）；草稿随任务走 |
| 5 | 删任务整目录 mv 归档 | 一次移动完成软归档，files/out 也入档可找回 |
| 6 | 无任务禁上传（不建收件箱） | 会话本就强制归属任务；禁用+提示最简 |
| 7 | 开发期清库切换、不写迁移 | 沿用 P4 先例（老数据清空重建） |
