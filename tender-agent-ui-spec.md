# Tender Agent — M3 UI 设计规范（三栏 · 轻快工具感 · 视觉+状态补全）

> **执行说明**：本里程碑为**纯前端改造**（外加一个 Tauri 打开文件所在目录的小 command），sidecar 与事件契约零改动。设计基调：轻快工具感（Notion/Linear 方向）——圆角卡片、柔和留白、单一强调色。**禁止**：自造颜色值（一律用 §1 tokens）、新增 API、动效层（仅保留 hover/transition 基础过渡）、更改既有数据流。

---

## 1. 设计 Tokens（唯一允许的视觉取值）

shadcn CSS 变量格式，直接替换 `globals.css` 主题：

```css
:root {
  --background: 240 20% 99%;        /* 页面底 #FAFAFB */
  --foreground: 240 10% 8%;         /* 正文 zinc-900 */
  --card: 0 0% 100%;                /* 卡片白 */
  --card-foreground: 240 10% 8%;
  --primary: 243 75% 59%;           /* 强调 indigo-600 #4F46E5 */
  --primary-foreground: 0 0% 100%;
  --secondary: 226 100% 97%;        /* 用户气泡底 indigo-50 #EEF2FF */
  --secondary-foreground: 243 60% 30%;
  --muted: 240 5% 96%;              /* 浅灰底 hover */
  --muted-foreground: 240 4% 46%;   /* 次要文字 zinc-500 */
  --border: 240 6% 90%;
  --input: 240 6% 90%;
  --ring: 243 75% 59%;
  --radius: 0.75rem;                /* 卡片 12px；小件用 8px */
}
--success: #10B981;  --warning: #F59E0B;  --error: #EF4444;
```

- 字体：系统栈 + Inter（`font-sans`）；正文 14px，聊天消息 15px，辅助文字 12–13px
- 间距：聊天区左右 padding 24px；卡片内 16px；组件间 12px
- 阴影：仅两档 `shadow-sm`（卡片）与 `shadow-md`（浮层）；禁用彩色阴影
- 图标：lucide-react 16/18px，stroke 1.75

## 2. 主布局骨架（三栏）

```text
┌──────────────────────────────────────────────────────────────────┐
│ ┌──────────┐ ┌────────────────────────────────┐ ┌──────────────┐ │
│ │ 左栏 240  │ │ 聊天区 flex-1（min 560px）      │ │ 产物栏 320    │ │
│ │          │ │                                │ │ 可折叠       │ │
│ │ ＋新对话  │ │ ┌ 顶栏：标题 ··· ────────────┐ │ │              │ │
│ │          │ │ └────────────────────────────┘ │ │ 产物卡片      │ │
│ │ 今天      │ │                                │ │ 产物卡片      │ │
│ │ ▸ 会话A   │ │  （消息滚动区）                 │ │ 产物卡片      │ │
│ │   会话B   │ │                                │ │              │ │
│ │ 更早      │ │                                │ │              │ │
│ │   会话C   │ │                                │ │              │ │
│ │──────────│ │ ┌ 输入区 ────────────────────┐ │ │              │ │
│ │ ● 正常    │ │ │ [📄chip]                   │ │ │              │ │
│ │ ⚙ 设置    │ │ │ 输入框…        📎  ➤       │ │ │              │ │
│ └──────────┘ │ └────────────────────────────┘ │ └──────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

规则：
- 左栏 240px、右栏 320px，均可折叠（折叠后以 44px 图标条或浮动按钮呈现，二选一实现）；折叠状态存 localStorage
- 窗口 < 1100px 时右栏自动收起为顶栏按钮
- 左栏底部两行：sidecar 状态指示（● 绿=正常 / ● 黄=重连中 / ● 红=失败）+ 设置入口
- 三栏之间用 1px border 分隔（不用阴影分栏），整体背景 `--background`

## 3. 左栏：会话列表

- 顶部「＋ 新对话」：主色描边按钮，hover 浅灰底
- 会话项：单行标题截断 + 相对时间（"2 小时前"）；分组标题「今天 / 昨天 / 更早」用 12px muted
- 状态：默认无底 → hover `--muted` → active 左侧 3px 主色竖条 + 浅灰底
- 会话项 hover 浮出 `···` 菜单：重命名 / 删除（删除需 confirm 对话框）

## 4. 聊天区：空状态（新对话）

```text
│                    │
│      欢迎使用 Tender Agent          │
│   上传招标文件，或直接开始对话          │
│                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐
│  │ 📄 解析招标 │ │ ⚖ 分析评分 │ │ 💬 我能做  │
│  │   文件目录  │ │   办法    │ │  什么     │
│  └──────────┘ └──────────┘ └──────────┘
│                                    │
```

- 垂直居中；标题 20px semibold，副标题 muted
- 三张建议卡：白底圆角卡片，hover 上浮 1px + border 主色；「解析招标文件目录」点击 = 打开文件选择器（选完直接上传并填入预设 prompt）；「分析评分办法」填入 tender-analysis 引导语；「我能做什么」填入能力询问
- 拖拽上传热区 = 整个聊天区（空态时同样生效）

## 5. 聊天区：消息流

- **用户消息**：右对齐，最大宽度 70%，`--secondary` 底圆角 12px（右上角 4px），内边距 10×14px
- **助手消息**：左对齐通栏（不套气泡），白底透明，15px 行高 1.7；markdown 表格用白底卡片包裹 + 12px 圆角
- **流式光标**：生成中文字末尾 2px 主色竖线闪烁
- 滚动：流式时自动贴底；用户上滚则停止贴底并浮出「↓ 回到底部」圆钮（右下角，距输入区 12px）
- 日期分隔线：跨天会话间插入居中 12px muted 日期（"8 月 23 日"）

## 6. 聊天区：过程可视化（进度 / 工具 / 产物）

```text
│ ▸ 已生成 2/5 · 解析招标文件 ▾            ← ProgressBadge
│   ┌────────────────────────────┐
│   │ ✓ 转换招标文件      convert_tender│  ← 工具卡（完成）
│   │   已生成 out/tender-full.md        │
│   ├────────────────────────────┤
│   │ ⟳ 规划响应文件目录     2/5 步     │  ← 工具卡（进行中）
│   └────────────────────────────┘
│ ┌────────────────────────────┐
│ │ 📄 tender-directory.html          │  ← 产物卡片
│ │ HTML · 120 KB · 点击预览          │
│ └────────────────────────────┘
```

- **ProgressBadge**：默认单行「已生成 2/5」+ 当前 todo 内容 + 展开箭头；点击展开完整清单（✓ 绿 / ⟳ 主色 / ○ 灰 三态）；全部完成后收起为一行 ✓
- **工具卡**：左侧 20px 状态图标（⟳ spinner 主色 / ✓ 绿 / ✗ 红）+ 工具显示名 + 右侧灰字工具真名；完成后第二行显示摘要（截断 1 行，hover 展开全量）；失败卡附带「查看错误」展开
- **产物卡片**：白底卡片，文件类型图标（html=🌐 json=🧾 md=📝，用 lucide 等价图标）+ 名称 + 类型 · 大小；整卡可点击 → 打开预览覆盖层；hover 显示「在文件夹中显示」图标按钮
- 工具显示名映射表（写死在前端）：`convert_tender→转换招标文件`、`extract_toc→抽取文档大纲`、`build_tender→组装产物`

## 7. 输入区

- 容器：白底圆角 16px 卡片 + `shadow-sm`，聚焦时 border 主色
- 多行输入：无边界 textarea，自适应高度（max 6 行后滚动）；`Enter` 发送 / `Shift+Enter` 换行；placeholder「输入消息，可上传招标文件…」
- 左下 📎 上传按钮；右下发送按钮（圆形主色，无内容时 disabled 灰）
- **生成中**：发送按钮变为 ■ 停止样式但**置灰不可点**（M3 无取消 API，tooltip「任务进行中」），输入可编辑但不可发送
- **文件 chip**：输入框上方一行，`📄 招标文件.docx · 256KB ✕`，白底描边圆角 8px；docx/doc/pdf 类型附加主色按钮「解析此文件」（点击填入预设 prompt）
- 上传中：chip 内换成进度条形态；失败：红边 chip + 重试按钮

## 8. 右栏：产物列表

- 顶栏「产物」标题 + 数量 badge + 折叠按钮
- 产物卡片：图标 + 名称（截断）+ 类型 · 相对时间；当前会话产出的卡片左上角主色小圆点标记
- 按时间倒序；hover 浮出「在文件夹中显示」
- **空态**：居中 illustration（lucide 文件夹图标 32px muted）+「还没有产物」+ 副文案「上传一份招标文件，让 Agent 生成目录规划」+ 主色按钮「上传文件」（=触发 📎）

## 9. 产物预览覆盖层

```text
┌──────────────────────────────────────────────┐
│ 📄 tender-directory.html   HTML · 120 KB       │
│                         [在文件夹中显示] [✕]    │
├──────────────────────────────────────────────┤
│                                              │
│              预览内容（居中 max-w 900px）      │
│   html → iframe（sandbox="allow-scripts"）    │
│   json → 格式化 <pre> 可复制                   │
│   md   → react-markdown                      │
│                                              │
└──────────────────────────────────────────────┘
```

- 居中模态：宽 min(90vw, 1000px)、高 85vh，白底圆角 16px，背景遮罩 black/40
- 顶栏：图标+名称+元信息，右侧两个按钮；`Esc` / 点遮罩关闭
- 加载中：骨架占位；加载失败：错误态 + 重试

## 10. Settings 对话框

轻改样式对齐 tokens（白卡圆角 16px、表单间距 16px），功能不变。保存成功 → 右下角 toast「已保存，正在重启服务…」。API Key 输入框：Tauri 环境显示，type=password + 显隐切换。

## 11. 全局状态清单（本次补全的核心）

| 状态 | 触发 | 表现 |
|---|---|---|
| 会话列表空 | 无会话 | 左栏空态文案「开始你的第一个对话」 |
| 消息加载 | 切换会话 | 3 条灰色骨架气泡 |
| 生成中 | agent.started→completed | §5 光标 + §7 发送锁定 + ProgressBadge |
| **sidecar 重连中** | 请求失败/健康检查超时 | 聊天区顶部黄条「⟳ 连接中断，正在重连…」，左栏 ● 黄 |
| **sidecar 恢复** | 重连成功 | 绿条「已恢复连接」自动 2s 消失，左栏 ● 绿 |
| **sidecar 失败** | 3 次重启失败 | 红条「服务启动失败」+「查看详情」+「重试」，左栏 ● 红 |
| 上传中/失败 | /api/files | §7 chip 进度 / 红 chip 重试 |
| 产物空态 | artifacts 空 | §8 空态 |
| 预览失败 | content 404/500 | §9 错误态 |
| 网络错误 | LLM 报错经 agent.error | 消息流内红边卡片显示错误 + 「重试」（重发上一条用户消息） |

## 12. Tauri 新增 command（唯一的 Rust 改动）

```rust
#[tauri::command]
fn reveal_in_folder(path: String) -> Result<(), String>
// 用 tauri-plugin-opener 的 reveal_item_in_dir；path 仅允许 data/workspace/ 下（做前缀校验）
```

## 13. 验收方式（截图对比，逐页用眼睛看）

每个编号章节（§2–§11）= 一个验收项：实现后截图与本文档 ASCII 布局逐项对照（结构、状态、token 取值），**不接受只查 DOM**。重点核对：三栏折叠往返、四种工具/进度状态、三色 sidecar 状态条、产物空态→列表→预览全链路。

## 14. 冻结边界重申

不新增任何 REST/SSE API；不做消息取消；不做动效层（仅 hover/focus transition ≤150ms）；不改 sidecar 与数据流；颜色只用 §1 tokens。
